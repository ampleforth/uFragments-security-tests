'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _Logger = require('../utils/Logger');

var _Logger2 = _interopRequireDefault(_Logger);

var _DistributionWrapper = require('./DistributionWrapper');

var _DistributionWrapper2 = _interopRequireDefault(_DistributionWrapper);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const log = new _Logger2.default('DistributionProvider');

exports.default = {
  from(address, txParams = {}) {
    this._fetchPackage(address);
    return new _DistributionWrapper2.default(this.package, txParams);
  },

  _fetchPackage(address) {
    log.info('Deploying new Package...');
    const Package = ContractsProvider.getFromLib('Package');
    this.package = new Package(address);
    log.info(`Deployed Package ${this.package.address}`);
  }
};